> 历史开发记录：以下条目按当时进度保留，后续条目可能已修正早期结论。当前能力与验收状态请以 [验证记录](VERIFICATION.md) 为准。

# 原项目功能对齐进度

来源：microcodex-short-drama-studio、story-image-agent、story-video-agent。

## 已发现并修正
- 独立生图入口丢失界面提示词：传入 scene.action / description。
- 独立视频入口使用占位 artifact：改用生图结果的真实首尾帧引用。
- 旧 get_media_run 查询自行推进状态：移除模拟成功，只返回后台状态。
- 视频适配器仅传 shot 时访问空 scene：增加兼容回退。

## 仍须完成与验证
- 故事服务生命周期与 capability：当前模板桥接没有启动原 Rust 宿主服务，真实生成未验证。
- 故事包已完整保存并按原场景建立镜头；仍需更细的场景内动作拆分、无对白角色识别。
- 独立生图尺寸已贯通；候选图选择、提示修订、真实质量评估与条件路径仍待贯通。
- 原图像适配器把同一图片作为首尾帧，需区分复用静帧与独立尾帧生成。
- 视频 Turbo 与固定步数已对齐；动作单元、关键帧条件、运行恢复和实际输出播放仍待对齐。
- MiniMax 工作流当前为 T2V，传递 frame_ref 不等于工作流已消费该图片。
- 图片、视频固定质量分数已移除；旧流水线 artifact://real 引用仍需接真实产物。
- 暂停仍未支持；服务地址与凭据读取需要统一核验。

此文记录未完成工作，不能作为端到端通过的证明。

## 参数核对补充
- 独立生图尺寸已传入原 provider._normalize_size。
- 独立视频 Turbo 已传入 ModelConfig；原 MiniMax 工作流步数固定为 Turbo 8 / 标准 20，界面改为显示实际步数，移除无效自由输入。

## 故事宿主核验
原 DesktopRunController.start 创建 CapabilityHost（generation / review / pricing），再用 SidecarProcess.launch_with_capability 启动服务；不是固定 8765 端口。当前移除了 run_media_stage 强制覆盖服务地址，读取环境或已有 settings.json；完整宿主生命周期仍待移植。
完整故事包与 scene_context 已进入 manifest，缓存随场景信息变化。图片与视频默认伪造质量分数已移除。

## 视频产物协议
原 ComfyUIAdapter.run_to_artifact 调用 store.put；已让 ArtifactRegistry.put 委托带哈希校验的 put_bytes，并用原 ComfyUIAdapter 的完成路径验证入库。该适配器仍没有持久提交收据，当前适配层已补 2 秒轮询间隔与超时任务 ID；持久恢复链路尚未完成。

## 最近集成验证
47 项 Python 测试通过，前端构建通过。测试包括原适配器的模拟完成路径，不代表真实云端故事、视频任务已成功执行。

## 页面切换与任务状态
- 流水线页面保持挂载，切换创作工具后保留项目、故事草稿、节点和运行中的请求。
- 独立故事页面首次访问后保持挂载，避免模板生成回调随着页面销毁而丢失。
- 流水线故事生成期间禁止切换/折叠节点，防止销毁生成组件。
- 浏览器交互验证：编辑流水线故事，切换故事工具再返回，正文与展开节点保持；前端构建通过。该验证不代表原故事服务已接通。

## 独立生成来源与失效状态
- 图片结果返回原 source_spans，视频入口沿用图片的来源标识，移除固定 scene-1；故事场景未提供来源列表时使用原 node_id。
- 重新生图开始即清除旧图片的就绪状态与旧质量信息，失败后不能继续误用上一张图；新故事完成后同步清除旧质量信息。
- 当前 Python 全量 49 项测试通过，前端构建通过。来源返回值已由适配器测试覆盖；云端实际执行仍待验证。

## 候选图选择
- 独立生图入口恢复原工作流 1–8 张候选数量，默认 1 张，高级设置折叠。
- 返回候选图片预览与真实 artifact，支持选择候选并传给视频；选择不等于质量审核通过。
- 批次中单张失败保留其余成功结果；当前未接单候选重试/持久批次恢复。
- 51 项 Python 测试通过；浏览器模拟服务验证第二张候选引用及原故事来源进入视频请求；前端构建通过。未调用付费生成。

## 独立视频播放
- 独立视频生成完成后，从内容寻址仓库校验哈希并导出 MP4/WebM 播放文件。
- 桌面只允许仓库 previews 目录内的返回文件通过 asset 协议读取，页面提供播放器和独立的播放错误反馈。
- 已验证损坏源产物拒绝播放、损坏导出副本可从有效原文件修复、未知容器拒绝伪装为 MP4。
- 53 项 Python 测试通过，前端构建与 cargo check 通过；真实 ComfyUI 输出的桌面播放尚未实测。

## 故事宿主自动启动
- 模板生成接入原 CapabilityHost + SidecarProcess.launch_with_capability，读取原生成/审核模型通道、价格表与 schema，保留宿主实例。
- 生成入口互斥，复用前检查服务健康；不因瞬时不可达自动重启。相同参数失败后重试沿用 job_id 以复用原幂等提交。
- Rust 常规 4 项测试通过，前端构建通过。
- 显式执行本机无模型请求的启动测试：失败于缺少原 deepseek Windows 凭据。尚未完成本机服务启动与实际生成验证；没有调用付费模型。

## 故事凭据设置入口
- 折叠的故事服务配置内可分别保存 DeepSeek / 阿里云审核凭据，使用原 WindowsCredentialStore 与 default profile。
- 密钥输入遮蔽，提交后清空，不写入项目或 localStorage；运行中禁止更改，现有宿主在应用重启后读取新凭据。
- 前端构建、Rust 检查通过；浏览器模拟验证保存参数、遮蔽输入与提交后清空。没有覆盖真实凭据；DeepSeek 缺失仍待用户配置。

## 视频恢复并发验证
- Windows 并发测试复现锁文件预读取 PermissionError；已改为直接获取系统字节锁，重复执行返回任务正在运行。
- 完成收据写入前核对 succeeded 状态、原 prompt_id 与产物完整性，异常结果保留 accepted 收据继续恢复。
- 缓存视频损坏时报错，不重新提交；并发请求仅提交一次。Python 全量 56 项测试通过。

## 旧全流程真实结果
- 修复全流程调用 StoryVideoAdapter 时 source_spans/story_spans 参数不匹配，原 node_id 可用于场景来源。
- 质量参数传给原适配器的生成调用，移除不支持的 plan 二次调用；仅有视频计划时保持 planned。
- Rust 旧接口移除 artifact://real，保留完整真实结果与错误，生图/视频引用来自实际结果；子进程失败时不猜测哪个阶段已完成。
- Python 57 项测试通过，Rust 4 项常规测试通过；本机宿主启动测试因缺少 DeepSeek 凭据默认忽略，此前显式运行已失败。

## 独立入口多场景选择
- 生图页面可选择完整故事包中的任意场景，使用该场景的动作/描述/对白与原 node_id。
- 视频提示词同步当前场景，切场清除旧候选就绪、旧播放与质量状态，避免跨场景误用素材。
- 浏览器模拟验证第二场进入生图请求、候选选择后正确传入视频；前端构建通过。每场结果的持久化管理尚未接入独立工具页面。

## 故事等待时限
- HTTP 等待按 story-job 的 deadline_seconds 扩展轮询次数，不再在默认约 600 秒时提前退出 900 秒任务；实际等待使用单调时钟边界。
- 超时反馈原 run_id 并提示同 job_id 恢复，不把超时等同任务失败；缺少 job_id 时移除所有请求共用固定幂等键的问题。
- 测试验证越过第 300 次轮询后仍能接收结果，以及超时仅提交一次并保留任务标识；Python 59 项通过。

## 无对白角色传递
- 对照原 story-package/v1：场景没有独立 cast 字段，角色由对白 speaker 引用或动作文本明确姓名识别。
- 保留无对白但动作中出现姓名的角色卡，记录 speaker_reference / action_name_match 来源；英文姓名防止子串误匹配，中文姓名属于文本启发式，代词和别名仍未推断。
- Python 全量 60 项测试通过，包含无台词角色与 Ann/Anna 误匹配用例。

## 独立视频恢复入口
- 视频请求保留输入快照，未完成时显示恢复上次任务按钮，恢复不读取后来修改的提示词或 Turbo 参数。
- 请求快照复用现有后端提交收据，未知提交继续阻止重复提交；当前快照仅在本次应用会话内保留。
- 浏览器模拟超时后修改提示词，验证恢复请求与原请求完全一致；前端构建通过。

## 跨会话视频请求恢复
- 视频提交前保存本地待恢复请求列表；保存失败不提交。多次任务记录分别保留，完成只移除对应输入。
- 页面启动读取待恢复列表，可使用原请求恢复，后端继续复用磁盘收据。未保存凭据或媒体内容。
- 浏览器模拟重新加载验证原请求恢复及完成清理，前端构建通过；桌面跨进程真实云端恢复尚未实测。

## 视频生成种子
- 原 MiniMax 工作流默认 seed=0，已恢复可编辑种子并贯通原图的 RandomNoise.noise_seed；高级参数默认折叠。
- 相同参数继续复用任务收据，修改种子可生成新版本；恢复快照保留原种子。
- Python 全量 61 项测试通过，包含原图参数断言；前端构建通过。

## 项目模板回显
- 模板组件监听传入的保存参数变化，打开已有项目时同步模板、篇幅、集数和预算，不再仅首次挂载读取。
- 普通输入编辑不触发重置；清空保存参数时恢复默认。
- 前端构建通过；浏览器模拟先展开故事节点再打开长篇项目，验证组件保持挂载时回显新模板与约束。

## 原生图提示词参数
- 独立生图高级参数恢复负面提示词、构图、连续性输入；空白沿用原默认值，切换场景读取对应场景参数。
- 使用原 ImagePromptWorkflow 验证负面提示词独立进入 negative_prompt，构图与连续性进入最终 prompt。
- Python 全量 62 项测试、前端构建通过；这些参数尚未加入主流水线逐镜头编辑。

## 原文引用格式
- 对照原 schema 的 span_ref 格式，故事包项目保留 story-package/scene-N 及其 action/dialogue 子引用，并传入生图。
- 独立入口与旧全流程的场景回退引用补 story-package 前缀；已有明确 source_spans 继续保留。
- Python 全量 63 项测试、前端构建通过。

## 配置保存不覆盖空白凭据
- 修复 save_settings 在令牌字段留空时随机覆盖 sidecar/capability 的行为；DashScope 留空也不再从旧文件覆盖已保存值。
- 只更新明确提交的非空凭据；令牌生成仍通过独立 generate_sidecar_token 接口。
- 新增纯逻辑测试，不读写真实凭据。

## 质量结果校验
- 外部质量服务返回必须包含有效决策；阶段不匹配、错误类型不合法时标记 unavailable，不显示为通过。
- 兼容未携带 stage 的旧响应，补入请求阶段。
- Python 全量 68 项测试通过；真实质量模型服务接入仍未完成，校验不是质量评估本身。

## 桌面发布构建
- 增加 custom-protocol 特性和 desktop:build 脚本，Release 内置前端，不依赖 Vite。
- `cargo build --release --features custom-protocol --manifest-path apps/desktop/src-tauri/Cargo.toml` 已成功。
- 当前旧调试窗口未关闭，新程序位于 target/release；尚未完成新程序的原生界面与在线生成验收。

## 原生 Release 验收
- 使用 Windows 原生窗口检查新版从 tauri.localhost 内置资源启动，并验证故事节点可展开。
- 实测发现 CSS 箭头乱码；入口 HTML 补 UTF-8、中文语言、viewport 与页面标题，并重新构建 Release。
- 仍未执行收费生成或完整项目创建播放验收。

### 候选图恢复记录（后端，待接界面）
- 对照 story-image-agent 的随机 request_id：每次 build_production_plan 都会创建新编号，不能用它跨进程恢复。
- 生图接口新增可选 batch_id，同批次按候选序号保存记录；仍调用原 provider 的提交、轮询、下载逻辑。
- 完成素材校验后复用；已接受任务复用 task_id；提交结果不明禁止自动重发；同批次修改参数拒绝恢复。
- 未传 batch_id 的旧调用保持原行为。桌面候选生成尚未传入批次编号，恢复按钮与批次持久化仍未完成，不能宣称桌面恢复已接通。
- 验证：python -m pytest -q，71 passed；新增断点恢复、同提示词不同候选、未知提交、参数改变和缓存损坏检查。未调用真实云端。

### 候选图恢复入口接入
- 桌面独立生图页提交前保存带随机 batch_id 的完整输入，失败或部分成功保留待恢复批次；每次新生成使用新批次。
- 恢复按钮重放原输入，不读取当前编辑参数；重启/刷新后从本地存储恢复列表，全部成功才移除。
- 验证：前端构建通过；Playwright 模拟接口验证刷新恢复、部分失败保留、成功清除及参数逐字一致。尚未进行真实云端断点端到端验收，也尚未重新打包桌面 Release。

### 原版生图适配器恢复验证与场景衔接
- 使用原 DashScopeImageProvider 和 ImagePromptWorkflow，在传输边界模拟任务状态：3 张中第 2 张超时，重建工作流后恢复，POST 总数仍为 3，仅第 2 张增加一次轮询，其他两张素材引用不变。
- 修复恢复其他场景候选图后视频动作仍用旧场景的问题：成功素材同步原请求动作，并清空旧视频预览。
- 验证：72 项 Python 测试通过；前端构建通过；浏览器模拟恢复后视频提示词为原批次场景。以上不等同真实云端验收。

### 故事原任务结果取回
- 原 server.start_run 根据持久事件中的 idempotency_key 返回原 acceptance，不重新执行任务。
- 模板组件提交前保存完整 story-job，提供未取回任务列表；刷新后可按原编号与参数取回，成功才删除，不保存凭据。
- 明确区分取回结果与重启执行；原服务进程终止后的任务续跑仍未补齐。
- 前端构建通过；浏览器模拟验证刷新后原 job 完整重放、成功移除记录。真实故事服务仍缺原 DeepSeek 凭据，未宣称模型生成验收完成。

### 视频规格参数恢复
- 原 build_minimax_h3_workflow 支持 width/height/length/fps，现已从折叠高级参数经 CLI 传入真实执行图；原默认 864×480、124 帧、24 fps 不变。
- 计划中的时长同步 length/fps，按钮显示实际估算时长；原请求快照自动包含规格，恢复不会读取修改后的参数。
- 73 项 Python 测试、前端构建通过；新增验证 480×864、192 帧、24 fps 进入原图且计划时长为 8 秒。尚未做真实 GPU 验收。
- 对照确认原仓库 DashScopeWanAdapter 已实现文生视频提交/轮询，当前主项目尚未连接此路由，仍待补齐。

### 原阿里云视频通道接入
- 复用原 DashScopeWanAdapter，CLI video_provider=dashscope 与桌面通道选择接入；默认仍为 ComfyUI。阿里云支持原模型、尺寸、时长、负面提示词参数，复用现有安全凭据读取路径。
- 任务凭据与视频素材落本地，接受后恢复查询原 task_id，提交未知不自动重复，完成缓存校验；下载请求不附模型凭据。
- 76 项 Python 测试、前端构建通过；验证轮询中断恢复、完成缓存、未知提交和非法时长。未真实调用云端、未验证当前模型服务可用性，也未重新打包 Release。
- 当前两路都是文生视频；首尾帧条件仍未接入，界面明确说明。

### 阿里云视频批次与通道恢复验收
- 原 DashScopeWanAdapter 集成测试确认模型、尺寸、时长、负面提示词进入原提交体，轮询中断后未新增提交。
- 修复相同参数无法生成新版本：阿里云新生成使用新 batch_id，恢复复用原批次；编号仅用于本地记录，不作为未知参数发送云端。
- 78 项 Python 测试、前端构建通过；浏览器模拟验证阿里云参数及批次传递，切换为 ComfyUI 后恢复仍完整重放原阿里云请求。

### 视频结果来源记录
- 两路成功视频结果保存为内容寻址 JSON，保留故事 source_spans、关联图片、实际生成参数和任务结果；返回 result_record_ref，关闭界面后仍能按引用检查。
- 关联图片明确标记 associated_image_refs，不伪称已用于 T2V 画面条件。输入字段采用白名单，不复制 API key 等凭据。
- 80 项 Python 测试通过，补充来源、默认参数和凭据排除验证；随后修正文本提示词的场景回退并通过 2 项针对性测试。上一轮浏览器测试进程已确认正常退出（exit 0）。

### 测试版原生启动验证
- 已启动 story-media-orchestrator-latest.exe，原生窗口可读取到完整流水线、项目选择、预览和底部操作，中文与箭头正常。
- sky 点击创作工具被工具的窗口归属检查拒绝：命中 msedgewebview2.exe / Chrome Legacy Window，而目标为 Tauri 外层窗口。激活并刷新后同样拒绝；键盘 Tab 焦点仍在宿主无标题文本。未将此工具定位失败认定为应用点击缺陷。
- 因此仅证明启动及首页渲染，未证明新增通道在原生窗口可操作。未触发模型调用，未关闭原用户窗口。

### 完整剧情导入修复
- 原结构化故事包同时有 summary 与 lines；旧模板界面优先 summary，导致正文显示和独立生图提示遗漏对白/动作。
- 模板生成与取回现在优先按原顺序导入非空 lines.text，缺少正文才回退动作/描述/摘要；独立场景生图同样不再让摘要覆盖已有完整文本。
- 前端构建通过；模拟原任务取回验证同时含摘要、动作、对白时，正文保留动作与对白。较长场景仍需要原动作单元拆分，尚未因此宣称完整镜头规划完成。

### 结构化故事按动作段落分镜
- 原视频 action_generator 是预定义动作控制，不是任意文本分镜器；未把预设动作当作剧情解析结果。
- 新创建的结构化故事项目按已有 action 行切分，每个动作保留其后对白；纯对白/摘要场景仍保留。镜头来源只引用对应行，角色上下文沿用整个场景，避免后续代词段落丢失角色。
- 原故事包保持不变，已有项目不自动重拆。81 项全量测试通过；角色上下文调整后 6 项结构化故事测试通过。自然语言单行多动作拆分仍未实现。

### 分镜正文与视觉动作分离
- 结构化镜头保存 visual_action（该动作段落），完整正文仍含对白，原故事包不改。
- 主流水线生图采用 visual_action，避免把台词当成画面动作；旧项目及没有动作行的镜头继续使用原正文，保持可恢复。
- 82 项 Python 测试通过，新增验证生图动作不含对白且正文完整。未声称已有素材会自动改善；新生成才使用新的视觉动作。

### 主流水线画面约束保留
- 故事场景的 framing/mood/negative/continuity 随镜头写入并读取 project.json，传递给原生图工作流，不再仅独立生图页支持。
- 分镜列表新增默认折叠的“画面与来源”，展示视觉动作、约束及原文引用，便于检查。
- 83 项 Python 测试、前端构建通过；新增保存/加载后参数完整到达适配器验证。尚未原生窗口交互验收与重新打包。

### 渲染素材进入最终合成
- 修复 render_preview 生成 shot.assets.video 后最终仍只拼图片的问题：存在 render 视频时先将视频/图片镜头规范成统一片段，再沿用最终音轨合成。
- 使用原 shot.duration，较短视频以末帧补足；图片镜头保留，临时片段成功/失败后清理，最终 MP4 仍原子替换。
- 84 项测试通过；真实 FFmpeg 用蓝色源图片、红色视频验证成片使用红色视频而非图片。
- 这是合成能力修复；桌面主流水线尚未选择并构造真实 video_provider，音频仍沿用现有 TTS/静音路径，不等同完整模型视频流水线已接通。

### 视频回退后的恢复修复
- render 模式配置视频 provider 时，图片回退不再满足完成缓存条件；恢复会重试缺失视频。
- 已完成图片与音频复用，避免视频恢复时重新合成语音；存在视频 provider 时取消自动连续重试，交给显式恢复和原任务凭据处理。
- 85 项测试通过；新增验证视频先失败回退、加载项目后恢复视频、图片和音频均未重做、旧错误清除。此项保留原代码允许预览回退的行为。

### 项目 render 模式连接模型视频
- CLI 显式 run --mode render 构造 ProjectVideoProvider，复用原 ComfyUI/MiniMax 适配器与项目 artifacts 中的任务凭据，按镜头视觉动作生成并物化视频文件，再进入混合合成。
- 普通 preview 模式保持不调用视频模型；初始化失败保存项目错误。ComfyUI 地址仍由原环境配置提供。
- 87 项测试通过，新增镜头视觉动作/来源/视频物化及 CLI 构造 provider 验证。桌面模式选择及 settings.json 地址传递仍待接入；没有做真实 GPU 生成。

### 桌面主流水线渲染入口
- 合成节点新增图片预览 / ComfyUI 模型视频选择；run 传递所选模式，resume 不覆盖已保存模式。创建项目响应不覆盖用户预先选择。
- Rust 项目命令将已保存 comfyui_url 注入原适配器环境（已有环境变量优先）。
- 前端构建、Rust 6 项测试通过（1 项外部故事服务测试忽略）；浏览器模拟验证创建后 run 收到 render。尚未真实 ComfyUI 验收和重新打包。

### 合成模式切换缓存修复
- 图片/音频缓存键不再包含输出模式（保持旧 preview 键）；精确匹配的旧 render 键迁移，内容与模型变化仍失效。
- preview 明确不进入视频片段合成；保留视频资产供以后切回 render 使用。
- 87 项测试通过；真实 FFmpeg 验证红色视频成片切回蓝色图片预览，图片未重新生成。

### 视频回退可见状态
- render 项目有 video fallback 镜头时，合成节点显示“视频未完成”，底部说明回退数量及恢复方式；合成详情逐镜显示错误。
- 保留可播放的回退预览，不再用项目 done 状态遮蔽视频失败。
- 前端构建和浏览器模拟验证通过（done 项目但视频失败时仍显示未完成、镜头编号及原因）。

### 完成镜头的离线重合成
- CLI render/resume 在素材及缓存键完整有效时不再初始化 ComfyUI，允许无服务地址时直接重合成本地视频。
- 缺素材、失败回退、正文/配置变化仍要求视频适配器；接受旧 render 缓存键的精确迁移。
- 88 项测试通过，新增验证已完成项目 resume 不构造视频服务，正文变更会重新要求生成。

### 原工作流完整恢复集成验收
- 新增 CLI 创建→render 中断回退→加载 resume→真实 FFmpeg 成片→离线重合成测试，使用原 VideoPromptWorkflow，仅模型执行替身。
- 暴露并修复原适配器错误调用受控 v2（缺控制包就拒绝）的缺陷：无 action_unit 使用原基础 build_pipeline，有动作单元才使用 v2；不伪造控制证据。
- 验证图片/音频修改时间不变、恢复成片为视频帧、第三次离线合成不初始化模型。全量 89 项测试通过。此证据不等同真实 GPU/云端生成。

### 基础视频工作流跨阶段兼容
- 修复 SingleSceneOrchestrator 仅接受 v2、拒绝原基础 v1 结果的问题；现在只接受原仓库明确支持的 v1/v2。
- 对应串联测试改用原 VideoPromptWorkflow，不再由 Mock 返回不真实的版本号。
- 全量 89 项测试通过；原基础视频流程返回 planned，未被误记为已生成。

### 原动作控制执行边界
- 使用原 classic_action_unit 与 VideoPromptWorkflow 验证内置动作可生成受控 v2 计划。
- 当前 MiniMax 执行图不消费动作控制包；适配器现在拒绝带动作单元的实际 ComfyUI 执行，防止生成普通 T2V 却暗示控制生效。计划生成保持可用。
- 全量 90 项测试通过。真正的控制计划执行器接入仍未完成，此处不作为动作控制功能完成证据。

### 静音镜头最小时长
- 查明短预览原因之一：FakeTTSProvider 原本最短 1 秒，覆盖 Shot 默认 3 秒；短句镜头会一闪而过。
- 新静音音轨最短 3 秒，长文本仍按长度延长；真实 TTS 接口不改。已有缓存音轨不自动重做。
- 新增短/长静音 WAV 时长验证；全量测试结果见本轮输出。模型视频实际时长与配音同步策略仍需进一步接入。

### 模型视频时长与逐镜音轨对齐
- ProjectVideoProvider 用本地 FFmpeg 读取视频实际时长，镜头取视频与音频较长值，避免短静音截断完整模型视频。
- 混合片段合成时逐镜补齐音轨到对应时长，临时音轨不改原缓存 WAV，防止后续镜头音频提前。
- 扩展真实 FFmpeg 恢复测试：5 秒视频对短静音，成片保留 125 帧（25 fps），缓存图片/音频不重做。全量测试结果见本轮输出。

### 图片预览音轨对齐
- 将逐镜音轨规范化独立出来，图片预览与视频渲染均按 shot.duration 补齐静音，防止切回图片后音轨提前。
- 92 项原测试通过；新增真实 FFmpeg 测试通过，逐样本验证 1 秒原 WAV 补至 3 秒且只追加静音，原文件不修改。

### 成片字幕轨
- 修复仅写旁路 subtitles.srt、MP4 不带故事文字的遗漏；最终编码显式映射视频/音频/SRT，并以 mov_text 保存可选字幕轨，保留独立 SRT。
- 真实 FFmpeg 提取成片字幕验证两段中文及 0–3/3–6 秒边界通过。是否在桌面播放器直接显示仍取决于其字幕轨支持，未宣称 WebView 字幕开关已接通。

### 桌面预览 WebVTT 字幕
- 主预览播放器根据已加载项目镜头生成 WebVTT，并加载“故事字幕”轨；切换项目与组件销毁时释放 Blob URL。
- 字幕文本转义，避免尖括号被当作字幕标记吞掉；时间按实际镜头 duration 累计。
- 前端构建通过；浏览器使用真实已有 MP4 验证字幕轨加载两条 cue、时间边界和 < > & 字面文字。原生 WebView 操作仍需单独验收。

### 角色剧情资料与画面提示分离
- 原故事角色包含 desire/fear/secret/contradiction/change 等剧情信息，主生图之前整块 JSON 拼入画面提示，容易挤占原 Wan 500 字限制。
- 新生成只将 name/appearance/clothing 的已有文本用于人物描述；完整角色数据仍保留，不编造外观。
- 新增原 ImagePromptWorkflow 验证：超长背景资料不进入画面提示，姓名/外观/服装保留，测试样例低于 500 字。并非保证所有长动作都在限制内，长动作仍需进一步拆分。

### 主项目阿里云视频路由
- 项目保存 video_provider，CLI --video-provider 与 Rust 项目命令、合成节点通道选择接通。阿里云复用原 Wan 适配器及任务凭据，默认原 5 秒/1280×720 模型参数。
- 切换通道清除视频关联而保留图片/音频；resume 沿用项目通道。
- 96 项 Python 测试、6 项 Rust 测试（1 忽略）、前端构建通过；浏览器模拟确认创建后 run 收到 render 与 dashscope。
- 主项目的详细视频模型参数还未开放；真实云端验收、首尾帧条件与模型语音仍待完成。

### 项目视频场景上下文
- 修复视频显式 prompt 仅有动作、原 scene_context 未进入模型文本的问题：追加已有地点/光线/时间/构图/氛围/连续性及精简人物外观。
- 不编造背景，不把角色秘密等剧情资料放入视觉提示；两条项目视频通道均获得同一组场景信息。
- 6 项项目视频及完整恢复测试通过，包括真实 FFmpeg 成片验收。已有视频不自动重做，新生成使用新的提示。


### 本轮原实现对照与验收更新
- 项目模型视频参数已接回合成节点，并保存 video_options；恢复沿用原值。按有效通道参数比较缓存，补默认值和修改非当前通道参数不会重做视频。
- 生图完整提示词修订复用 ImagePromptWorkflow.revise_prompt，保存父子版本与 source_spans/negative_prompt；界面可看当前版本及最近 20 批文字历史，复用只修改草稿。
- 原 MiniMax H3 图包含 VAEDecodeAudio → CreateVideo.audio。合成已恢复有声视频的嵌入音轨；无声视频回退缓存音轨。真实 FFmpeg 端到端测试覆盖最终 MP4 有声以及切回图片预览无模型残留音频。
- 重新执行 original_host_starts_without_generation --ignored：原运行时启动失败，缺少 deepseek 凭据。未发起模型请求，不能以模拟测试声称真实故事生成可用。
- 三个原仓库的 Python/Rust/TOML/Markdown 源码检索未发现独立 TTS/CosyVoice/配音实现；模型视频自带声音不等于按脚本生成对白配音。
- 尚待：真实故事/云端/GPU 全流程验收，主流水线候选图选择与质量评估完整衔接，首尾帧与动作控制执行、暂停/取消、独立发行包，以及最新改动的桌面 Release 更新。上述项未完成，整体目标保持进行中。


### 阿里云真实视频验收完成（本机重测）
- 用户明确要求自行重新测试后，使用新的验收批次提交。国际区模型 wan2.1-t2v-turbo 在北京接口被明确拒绝：HTTP 400 InvalidParameter: Model not exist；rejected 回执保留。
- 对照官方中国区说明，改用 wanx2.1-t2v-turbo，任务 1df773fd-66ea-4329-b687-5763f7f7a4eb 成功。生成参数 5 秒、1280*720，实际文件 5.366667 秒、1280x720、30fps、H.264。
- 结果位于 .preview-smoke/wan-live-retest/result.json，完整 FFmpeg 解码通过；浏览器实际播放 currentTime > 1，error=null。
- 恢复检查将 submit/wait 设为禁止调用，仍取回同一 artifact，证实已完成任务不重提。
- 桌面与 Python 默认模型已改为北京区名称；两种地区名称的 5 秒校验均保留。全量 Python 126 项通过。
- 此次只证明阿里云单条视频的提交、下载、解码、浏览器播放和完成后缓存恢复；不代表故事生成、整条桌面流水线和所有原功能均已验收。
